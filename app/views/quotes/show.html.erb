<h1 class="font-bold text-2xl"><%= @quote.total_gross_weight %> kg Quote</h1>

<hr class="mt-3 mb-7" />

<div class="flex flex-wrap gap-4 justify-center">
  <%= render "show", quote: @quote %>
</div>

<hr class="mt-7 mb-3" />

<%= link_to "Edit quote", edit_quote_path(@quote), class: "rounded font-medium w-full sm:w-auto px-5 py-2 text-center text-white bg-green-700 inline-block text-sm font-medium" %>

<%= link_to "Back to quotes", quotes_path, class: "rounded font-medium w-full sm:w-auto px-5 py-2 text-center bg-gray-100 inline-block text-sm font-medium" %>
